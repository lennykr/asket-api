# Asket REST API

## Setting up a development environment

### Import dependencies
```bash
    yarn
```

### Setup a .env file
```bash
    cp ./.env.example ./.env
```
> You can base your `.env` file on the example included in the repository.

### Run the Node.JS API (via nodemon) with the following command:
```bash
    yarn start
```